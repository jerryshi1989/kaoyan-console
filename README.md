# 上岸操作台 · 管理类联考学习系统

这是一个纯静态网页学习工具，适合使用 GitHub Pages 发布。

## 本地打开

在项目目录运行：

```bash
python3 -m http.server 4173
```

然后访问 <http://127.0.0.1:4173/>。

## GitHub Pages

将仓库发布分支设置为 `main`，目录设置为 `/ (root)`。发布完成后，访问：

`https://你的用户名.github.io/仓库名/`

